#!/usr/bin/env bash
# Reminds agent to verify code facts from source, not training data memory
echo "VERIFY RULE: Before stating anything about function signatures, file paths, API shapes, event types, env vars, or field names — grep/read the source first. Training data is stale. Code is truth."
