#!/usr/bin/env bash
# Interactive Slack alerts pane.
# - new alerts blink until acked
# - any key acks (clears the blink)
# - q / Ctrl-C exits
exec /usr/bin/env python3 "$HOME/.dotfiles/scripts/slack-tldr.py" watch
